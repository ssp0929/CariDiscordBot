const dabMap = {
  dab: true,
};
const haegMap = {
  hell: true,
  heck: true,
};
const postureMap = {
  posture: true,
};
const nutMap = {
  nut: true,
};
const buttMap = {
  butt: true,
  ass: true,
};

export {
  dabMap,
  haegMap,
  postureMap,
  nutMap,
  buttMap,
};
